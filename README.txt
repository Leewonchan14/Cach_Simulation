캐시 시뮬레이션에서 성능(Perfomance)는 (Total_Hit/Total_Cycle)*1000 으로 정의하였다.

1. Cach_Simulator.py 파일은 실행인자로 크기와 write타입들을 받는 파이썬 파일이다.

2. Flow.py 파일은 주석으로 캐시 시뮬레이션의 흐름을 나타낸 파일이다.

3. Simulation_Function.py 파일은 본인이 성능 실험 을 하기 위해 사용자 정의 함수로 Cach_Simulator.py를 만든 파일이다.(신경 x)
